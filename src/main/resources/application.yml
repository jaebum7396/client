spring:
  application:
    name: user
  profiles:
    #active: local
    active: deploy
  mvc:
    view:
      prefix: /WEB-INF/view/
      suffix: .jsp
  jsp:
    cache: 'false'
  devtools:
    restart:
      enabled: 'true'
    livereload:
      enabled: 'true'

backend:
  url: ${BACKEND_URL}

